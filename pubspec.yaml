name: medigo
description: "medigo"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  smooth_page_indicator:
  provider:
  camera: ^0.11.2
  flutter_map:
  latlong2:
  chat_bubbles: ^1.7.0
  image_picker: ^1.0.7
  cupertino_icons: ^1.0.8
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
  assets:
    - "assets/"
